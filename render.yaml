services:
  - type: web
    name: stock-monitor
    runtime: python
    plan: free
    buildCommand: pip install -e .
    startCommand: python -m stock_monitor
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: STOCK_MONITOR_HOST
        value: 0.0.0.0
